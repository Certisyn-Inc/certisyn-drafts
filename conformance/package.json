{
  "name": "arp-conformance",
  "version": "0.0.0",
  "private": true,
  "description": "Dependencies for the Node runners in this conformance tree. Declared here so that the npm install in REPRODUCE.md Section 3 lands in this directory rather than walking up into whatever repository happens to be above it.",
  "dependencies": {
    "@noble/post-quantum": "0.6.1"
  }
}
